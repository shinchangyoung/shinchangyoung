
```py
#@title 🎯 G:\내 드라이브\shin 정확 경로 확인 & 마운트
from google.colab import drive
drive.mount('/content/drive')

# Windows "G:\내 드라이브\shin" → Colab 경로 변환
model_folder = "/content/drive/MyDrive/shin"  # 한글 폴더명도 OK
model_path = f"{model_folder}/sd_xl_base_1.0.safetensors"

# 파일 존재 확인
import os
print("📁 폴더 탐색 중...")
!ls -la "{model_folder}"

if os.path.exists(model_path):
    print(f"✅ 발견! {model_path}")
    print(f"   📏 크기: {os.path.getsize(model_path)/1e9:.1f} GB")
else:
    print("❌ 파일 없음. 폴더에 있는 파일 목록:")
    !ls -la "{model_folder}" | grep -i sdxl

```

